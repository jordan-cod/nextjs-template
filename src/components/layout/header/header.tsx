export default function Header(): React.ReactNode {
    return (
        <header className="width-full h-20 bg-black text-white p-4 flex justify-between items-center">
            Header
        </header>
    );
}
